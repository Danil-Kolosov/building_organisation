@page
@model ConstructionOrganisation.Pages.Reports.sp_get_management_machines
@{
    ViewData["Title"] = "Техника строительного управления";
}

<div class="container mt-4">
    <h2>@ViewData["Title"]</h2>

    <form method="get" class="mb-4">
        <div class="row">
            <div class="col-md-4">
                <div class="form-group">
                    <label for="managementNumber">Номер управления:</label>
                    <input type="number" class="form-control" id="managementNumber" 
                           name="managementNumber" value="@Model.ManagementNumber" />
                </div>
            </div>
            <div class="col-md-4 align-self-end">
                <button type="submit" class="btn btn-primary">Показать</button>
            </div>
        </div>
    </form>

    @if (Model.ReportData != null && Model.ReportData.Any())
    {
            <div class="table-responsive">
                <table class="table table-striped table-bordered">
                    <thead class="thead-dark">
                        <tr>
                            <th>Серийный номер</th>
                            <th>Тип техники</th>
                        </tr>
                    </thead>
                    <tbody>
                    @foreach (var item in Model.ReportData)
                    {
                                <tr>
                                    <td>@item.SerialNumber</td>
                                    <td>@item.MachineType</td>
                                </tr>
                    }
                    </tbody>
                </table>
            </div>
    }
    else if (Model.ReportData != null)
    {
            <div class="alert alert-warning">Техника не найдена</div>
    }
</div>